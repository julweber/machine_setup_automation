# Kestra secrets — do not commit, do not expose.
# Resolved at runtime by docker compose from this file (mode 600);
# docker-compose.yml keeps only literal ${VAR} placeholders.
KESTRA_USERNAME=${KESTRA_USERNAME}
KESTRA_PASSWORD=${KESTRA_PASSWORD}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
