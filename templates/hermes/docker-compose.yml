services:
  hermes-gateway:
    image: nousresearch/hermes-agent:latest
    container_name: hermes-gateway
    volumes:
      - /srv/hermes/.hermes:/opt/data
      - /srv/hermes/workspace:/root/workspace
    restart: unless-stopped
    command: ["gateway"]

  hermes-chat:
    image: nousresearch/hermes-agent:latest
    profiles:
      - manual
    container_name: hermes-chat
    volumes:
      - /srv/hermes/.hermes:/opt/data
      - /srv/hermes/workspace:/root/workspace
    stdin_open: true
    tty: true
    command: ["chat"]

  hermes-setup:
    image: nousresearch/hermes-agent:latest
    profiles:
      - manual
    container_name: hermes-setup
    volumes:
      - /srv/hermes/.hermes:/opt/data
      - /srv/hermes/workspace:/root/workspace
    stdin_open: true
    tty: true
    command: ["setup"]
